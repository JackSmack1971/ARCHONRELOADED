{
  "dashboard": {
    "title": "Embedding Services Overview",
    "panels": [
      {
        "type": "graph",
        "title": "Request Latency",
        "datasource": "${DS_PROMETHEUS}",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=\"embedding-services\"}[5m])) by (le))"
          }
        ]
      }
    ]
  }
}
